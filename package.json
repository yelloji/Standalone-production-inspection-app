{
  "name": "standalone-production-inspection-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "electron"
  ],
  "engines": {
    "node": ">=22.12 <25",
    "npm": ">=10 <12"
  },
  "scripts": {
    "dev": "concurrently --kill-others --names backend,frontend,electron \"npm:dev:backend\" \"npm:dev:frontend\" \"npm:dev:electron\"",
    "dev:backend": "python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:electron": "npm run dev --workspace electron",
    "build": "npm run build --workspace frontend && npm run build --workspace electron",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace electron",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace electron",
    "test": "npm run test --workspace frontend && npm run test --workspace electron",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "concurrently": "9.2.4"
  }
}
